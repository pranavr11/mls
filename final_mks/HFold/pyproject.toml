[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hfold"
version = "0.1.0"
description = "Reference PyTorch implementation of HFold attention."
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["hfold*"]
